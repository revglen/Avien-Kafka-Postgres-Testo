from setuptools import setup, find_packages

setup(
    name="fraud_transaction_simulator",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # your dependencies
    ],
)